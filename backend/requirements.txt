# FluGuard AI Backend Dependencies — Railway (Cloud) Edition
# 流感卫士 AI 后端依赖 — Railway 云端部署版
#
# Audio features (cough detection, voiceprint) are excluded here to keep the
# Docker image under Railway's 4 GB free-tier limit. Those features work
# fully in local development — see requirements-audio.txt.

# ── Web Framework ──────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# ── HTTP Client (for Open-Meteo weather API) ─────────────────────────────
httpx>=0.28.0

# ── Data Validation ───────────────────────────────────────────────────────
pydantic>=2.0.0

# ── Google AI Studio SDK (Gemma 4 inference) ─────────────────────────────
google-genai>=1.0.0

# ── RAG: Vector Store ─────────────────────────────────────────────────────
chromadb>=0.5.0

# ── RAG: Embeddings (multilingual, supports Chinese + English) ───────────
sentence-transformers>=3.3.0

# ── File Upload Support ────────────────────────────────────────────────────
python-multipart>=0.0.9

# ── Utilities ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
